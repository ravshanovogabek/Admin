import React from 'react';
import './models.css';

const Models = () => {
    return (
        <div className="models">
            <h1>Models</h1>
            <p>Manage models here.</p>
            {/* Add table or form for models management */}
        </div>
    );
};

export default Models;
